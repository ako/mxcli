{
  "content": [
    {
      "text": "{\"kind\":\"constructor\",\"schema\":\"// Entity in the domain model. CRTICIAL: Set only one of generalization or isPersistable properties. To add validation rules, ask for DomainModels$ValidationRule schema. To add indices, ask for DomainModels$Index schema.\\nconstructor type 'DomainModels$Entity' = {\\n $Type: 'DomainModels$Entity',\\n // The name of the entity.\\n name?: string;\\n attributes?: PlainObject<{\\n  name?: string;\\n  type: 'Binary' | 'Boolean' | 'DateTime' | 'HashedString' | 'String' | 'Decimal' | 'Integer' | 'Long' | 'AutoNumber' | 'Enumeration' = \\\"String\\\";\\n  // Set this when attribute type is 'Enumeration' to specify which enumeration to use -- use fully qualified name. Make sure that the enumeration already exists (create new one if necessary).\\n  enumerationName?: Reference<'Enumerations$Enumeration', 'qualified-name'>;\\n }>[];\\n // Entity's position in the Studio Pro canvas.\\n location?: point;\\n // If set, indicates the parent entity this entity extends. If not set, this is a root entity. Example: MyFirstModule.MyParentEntity\\n generalization?: Reference<'DomainModels$Entity', 'qualified-name'>;\\n // If false, this entity will be marked as non-persistable entity. Non-persistable entities are stored in the runtime memory and never get committed to the database. Therefore, they have no table in the database and the only way to retrieve them is over associations. CRTICIAL: This field will only be considered if the entity has no generalization.To set the persistability of the entity after creation, check its generalization property: if it is of NoGeneralization type, you can set its persistable property; if it is of Generalization type, you need to follow inheritance chain and set persistable property on the root ancestor entity that has NoGeneralization as its generalization type -- this will make all the descendants in the inheritance chain non-persistable.\\n isPersistable?: boolean = true;\\n // System-managed attributes added as database columns. Never set these properties unless the user explicitly asks for them.\\n systemAttributes?: PlainObject<{\\n  // If true, the entity will have a 'changedDate' system attribute that is automatically set to the date and time when the object was last changed.\\n  hasChangedDate?: boolean = false;\\n  // If true, the entity will have a 'createdDate' system attribute that is automatically set to the date and time when the object was created.\\n  hasCreatedDate?: boolean = false;\\n  // If true, the entity will have an 'owner' system attribute that references the user who created the object.\\n  hasOwner?: boolean = false;\\n  // If true, the entity will have a 'changedBy' system attribute that references the user who last changed the object.\\n  hasChangedBy?: boolean = false;\\n }>;\\n // If set, marks this as a view entity backed by an OQL query. Provide a DomainModels$OqlViewEntitySource element with a sourceDocument reference to the ViewEntitySourceDocument that holds the OQL.\\n source?: Element<'DomainModels$OqlViewEntitySource'>;\\n}\\n\\n// Represents an entity whose data is sourced from an OQL view document.\\nelement type 'DomainModels$OqlViewEntitySource' = {\\n $Type: 'DomainModels$OqlViewEntitySource',\\n // The ID of the OQL view document that defines the source.\\n sourceDocument?: Reference<'DomainModels$ViewEntitySourceDocument', 'qualified-name'>;\\n}\"}",
      "type": "text"
    }
  ],
  "isError": false
}